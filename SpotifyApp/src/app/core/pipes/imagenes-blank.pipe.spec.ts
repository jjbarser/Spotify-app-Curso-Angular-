import { ImagenesBlankPipe } from './imagenes-blank.pipe';

describe('ImagenesBlankPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenesBlankPipe();
    expect(pipe).toBeTruthy();
  });
});
